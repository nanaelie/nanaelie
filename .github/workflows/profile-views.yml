name: Profile views counter
on:
  schedule:
    - cron: '0 * * * *'  # Mise à jour toutes les heures
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Update profile views
        uses: nanaelie/my-profile-views-counter@master
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
