name: Profile views counter
on:
  schedule:
    - cron: '0 * * * *'  # Mise à jour toutes les heures
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Update profile views
        uses: gayanvoice/my-profile-views-counter@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
